{
  "name": "leaflet-map",
  "version": "0.1.0",
  "description": "Simple library for creating a Leaflet map",
  "author": "dpet23",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dpet23/leaflet-map.git"
  },
  "keywords": ["leaflet", "leaflet-map", "maps"],
  "main": "index.ts",
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^4.9.4"
  }
}
