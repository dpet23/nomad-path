# Data contract

> Placeholder — the concrete schema is defined in implementation phase 2 (see `plans/`). This page will document:

- The emitted file's top-level shape and versioning.
- The flat-item model: geometries, parallel-array coordinates and per-point attributes, pipeline-stamped fields (group label, day, divider, default visibility, ordering).
- Optional vs required attributes, and why "absent optional" is a valid state.
- The semantic validation the pipeline runs before publishing.
